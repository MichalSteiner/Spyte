// Function to trigger the click event of the save button
export function triggerSaveButton() {
    const saveButton = document.getElementById('save-button');
    if (saveButton) {
        saveButton.click();
    }
}
